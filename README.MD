1️⃣ What is the difference between var, let, and const?

var: this is the easy away and it can used everywhere in code but sometimes its create bug and problem.
let: when its used then we can change value anytime.
const: when its used then we can not change value.

2️⃣ What is the spread operator (...)?

spread operator is that type of operator which can copy array easily and combine different array and object.also uses this operator added elements anywhere in array.

3️⃣ What is the difference between map(), filter(), and forEach()?

map(): it can loop through of an array and also return a modified new array.
filter(): it can create a new array with depending condition and pass a new array.
forEach(): it can used to loop through of an array 

4️⃣ What is an arrow function?

arrow function is that function which is the ES6 version of javascript. it is the modern and easiest away for uses.where code is too short.

5️⃣ What are template literals?

template literals is the modern away for text or string of javascript.its actually backticks, which is the opposite away of single or double quotation. its can used for multiline code and dynamically added html on javascript.